import { useState } from "react";
import { ArrowRight, Radio } from "lucide-react";

const API=`${process.env.REACT_APP_BACKEND_URL}/api`;
export const SignalSignup=()=>{const[email,setEmail]=useState("");const[interest,setInterest]=useState("Everything");const[status,setStatus]=useState("");const submit=async(event)=>{event.preventDefault();setStatus("sending");const response=await fetch(`${API}/subscribers`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,interest})});if(response.ok){setStatus("saved");setEmail("")}else setStatus("error")};return <section className="signal-signup" data-testid="signal-signup-section"><div><Radio/><p className="kicker">The Signal</p><h2>One useful idea.<br/>No spiritual noise.</h2><p>A concise field note connecting everyday life with consciousness, symbolism, tradition, and grounded practice.</p></div><form onSubmit={submit} data-testid="signal-signup-form"><label>What interests you most?<select value={interest} onChange={(event)=>setInterest(event.target.value)} data-testid="signal-interest-select"><option>Everything</option><option>Self-discovery</option><option>Spiritual meanings</option><option>Traditions & metaphysics</option><option>Holistic practice</option></select></label><label>Your email<input required type="email" value={email} onChange={(event)=>setEmail(event.target.value)} placeholder="you@example.com" data-testid="signal-email-input"/></label><button type="submit" data-testid="signal-submit-button">{status==="sending"?"Joining…":"Join The Signal"}<ArrowRight/></button>{status==="saved"&&<p className="signup-success" data-testid="signal-success-message">You’re in. The signal—not the noise.</p>}{status==="error"&&<p className="action-error" data-testid="signal-error-message">That address could not be saved. Please check it and try again.</p>}<small>Joining gives SHANE-AI permission to email you. Unsubscribe anytime.</small></form></section>};